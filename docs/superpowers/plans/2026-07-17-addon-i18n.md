# Rebalancer Addon i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Localize the Rebalancer addon and follow the Wealthfolio host's configured language (en/fr/de/es/zh) automatically and reactively.

**Architecture:** The addon runs its own isolated i18next instance with all locale JSON bundled into the single `addon.js`. It reads the host language from `ctx.api.settings.get().language` via the shared React Query cache (key `settings`), so it re-translates live when the host language changes. `i18next-cli` extracts keys from code and generates typed key definitions.

**Tech Stack:** i18next, react-i18next (bundled runtime), i18next-cli (dev tooling), @tanstack/react-query (shared cache), vitest + jsdom + react-dom `createRoot` (tests).

## Global Constraints

- Supported languages, verbatim: `['en', 'fr', 'de', 'es', 'zh']`. Fallback language: `en`.
- Single namespace: `rebalancer` (also `defaultNS`).
- Translation resources MUST be bundled/inlined — no lazy chunks, no HTTP/backend loading (the host dev loader `eval`s a single `addon.js`; dynamic import chunks do not resolve).
- Language access is guarded: any failure (permission denied, thrown call, absent/unsupported value) falls back to `en` and never crashes.
- `i18next-cli` requires **Node.js >= 22**. Confirm `node -v` before installing.
- Follow existing repo patterns: React Query hooks use `queryKey: [QueryKeys.X]` + `queryFn: () => ctx.api.X.method()` (see `src/hooks/use-accounts.ts`). Tests use `// @vitest-environment jsdom` + `createRoot`/`act` (no @testing-library). Lint/format via Biome; type-check via `tsc --noEmit`.
- Primary-language JSON values come from `t('key', 'English default')` synced with `i18next-cli extract --sync-primary`. Secondary languages get empty placeholders until filled.

---

### Task 1: i18n foundation (instance, config, tooling, provider)

**Files:**
- Modify: `package.json` (dependencies + scripts)
- Modify: `tsconfig.json` (ensure `resolveJsonModule`)
- Create: `src/i18n/i18n.ts`
- Create: `src/i18n/locales/en/rebalancer.json`
- Create: `src/i18n/locales/fr/rebalancer.json`
- Create: `src/i18n/locales/de/rebalancer.json`
- Create: `src/i18n/locales/es/rebalancer.json`
- Create: `src/i18n/locales/zh/rebalancer.json`
- Create: `i18next.config.ts`
- Modify: `src/addon.tsx` (wrap in `I18nextProvider`)
- Modify: `lefthook.yml` (extract + types gates)
- Test: `src/i18n/i18n.test.ts`

**Interfaces:**
- Produces:
  - `src/i18n/i18n.ts` default export: a configured i18next instance (`import type { i18n } from 'i18next'`).
  - Named exports: `SUPPORTED_LANGUAGES: readonly ['en','fr','de','es','zh']`, `DEFAULT_LANGUAGE: 'en'`, `NS: 'rebalancer'`.
  - `package.json` scripts: `i18n:extract`, `i18n:types`, `i18n`.

- [ ] **Step 1: Confirm Node version**

Run: `node -v`
Expected: `v22.x` or higher. If lower, stop and switch Node (the repo's `.node-version` / nvm) before continuing.

- [ ] **Step 2: Install dependencies**

Run:
```bash
pnpm add i18next react-i18next
pnpm add -D i18next-cli
```
Expected: `i18next` and `react-i18next` appear under `dependencies`; `i18next-cli` under `devDependencies` in `package.json`.

- [ ] **Step 3: Ensure `resolveJsonModule` in tsconfig**

Open `tsconfig.json`. In `compilerOptions`, ensure both are present (add if missing):
```jsonc
"resolveJsonModule": true,
"esModuleInterop": true,
```

- [ ] **Step 4: Create the five locale files (empty objects)**

Create each of these with exactly `{}`:
- `src/i18n/locales/en/rebalancer.json`
- `src/i18n/locales/fr/rebalancer.json`
- `src/i18n/locales/de/rebalancer.json`
- `src/i18n/locales/es/rebalancer.json`
- `src/i18n/locales/zh/rebalancer.json`

```json
{}
```

- [ ] **Step 5: Create the i18n instance**

Create `src/i18n/i18n.ts`:
```ts
import i18next from 'i18next';
import { initReactI18next } from 'react-i18next';
import de from './locales/de/rebalancer.json';
import en from './locales/en/rebalancer.json';
import es from './locales/es/rebalancer.json';
import fr from './locales/fr/rebalancer.json';
import zh from './locales/zh/rebalancer.json';

export const SUPPORTED_LANGUAGES = ['en', 'fr', 'de', 'es', 'zh'] as const;
export const DEFAULT_LANGUAGE = 'en';
export const NS = 'rebalancer';

const instance = i18next.createInstance();

instance.use(initReactI18next).init({
  lng: DEFAULT_LANGUAGE,
  fallbackLng: DEFAULT_LANGUAGE,
  supportedLngs: [...SUPPORTED_LANGUAGES],
  load: 'languageOnly',
  ns: [NS],
  defaultNS: NS,
  resources: {
    en: { rebalancer: en },
    fr: { rebalancer: fr },
    de: { rebalancer: de },
    es: { rebalancer: es },
    zh: { rebalancer: zh },
  },
  interpolation: { escapeValue: false },
  react: { useSuspense: false },
});

export default instance;
```

- [ ] **Step 6: Write the failing smoke test**

Create `src/i18n/i18n.test.ts`:
```ts
import { describe, expect, it } from 'vitest';
import i18n, { DEFAULT_LANGUAGE, NS, SUPPORTED_LANGUAGES } from './i18n';

describe('rebalancer i18n instance', () => {
  it('initializes in the default language', () => {
    expect(i18n.language).toBe(DEFAULT_LANGUAGE);
  });

  it('exposes the rebalancer namespace and supported languages', () => {
    expect(NS).toBe('rebalancer');
    expect(SUPPORTED_LANGUAGES).toEqual(['en', 'fr', 'de', 'es', 'zh']);
  });

  it('returns the key itself for a missing translation without throwing', () => {
    expect(i18n.t('does.not.exist')).toBe('does.not.exist');
  });
});
```

- [ ] **Step 7: Run the test to verify it passes**

Run: `pnpm vitest run src/i18n/i18n.test.ts`
Expected: 3 passing tests.

- [ ] **Step 8: Wrap the addon tree in `I18nextProvider`**

Modify `src/addon.tsx`. Add imports and wrap:
```tsx
import { type QueryClient, QueryClientProvider } from '@tanstack/react-query';
import type { AddonContext } from '@wealthfolio/addon-sdk';
import { I18nextProvider } from 'react-i18next';
import i18n from './i18n/i18n';
import { addonMainRouteId, addonName } from './lib';
import { SelectedAccountProvider } from './lib/account-provider';
import { Rebalancer } from './pages/rebalancer';

const RebalancerWrapper = ({ ctx }: { ctx: AddonContext }) => {
  const sharedQueryClient = ctx.api.query.getClient() as QueryClient;

  return (
    <QueryClientProvider client={sharedQueryClient}>
      <I18nextProvider i18n={i18n}>
        <SelectedAccountProvider ctx={ctx}>
          <Rebalancer ctx={ctx} />
        </SelectedAccountProvider>
      </I18nextProvider>
    </QueryClientProvider>
  );
};
```
(Leave the `enable` export unchanged.)

- [ ] **Step 9: Create the i18next-cli config**

Create `i18next.config.ts`:
```ts
import { defineConfig } from 'i18next-cli';

export default defineConfig({
  locales: ['en', 'fr', 'de', 'es', 'zh'],
  extract: {
    input: ['src/**/*.{ts,tsx}'],
    output: 'src/i18n/locales/{{language}}/{{namespace}}.json',
    defaultNS: 'rebalancer',
    primaryLanguage: 'en',
    sort: true,
    indentation: 2,
  },
  types: {
    input: ['src/i18n/locales/en/*.json'],
    output: 'src/types/i18next.d.ts',
    resourcesFile: 'src/types/resources.d.ts',
  },
});
```

- [ ] **Step 10: Add package.json scripts**

In `package.json` `scripts`, add:
```json
"i18n:extract": "i18next-cli extract --sync-primary",
"i18n:types": "i18next-cli types",
"i18n": "i18next-cli extract --sync-primary --with-types",
```

- [ ] **Step 11: Add lefthook gates**

In `lefthook.yml`, under `pre-commit.commands`, add two commands (siblings of `biome` and `type-check`):
```yaml
    i18n-extract:
      glob: "*.{ts,tsx}"
      run: pnpm exec i18next-cli extract --sync-primary --ci
      fail_text: |
        Translation files are out of sync with the code.
        Run 'pnpm i18n:extract' to update them.
    i18n-types:
      glob: "*.{ts,tsx}"
      run: pnpm exec i18next-cli types --ci
      fail_text: |
        Translation types are out of date.
        Run 'pnpm i18n:types' to regenerate them.
```

- [ ] **Step 12: Verify the extractor and type generator run**

Run: `pnpm i18n`
Expected: command completes without error; `src/types/i18next.d.ts` and `src/types/resources.d.ts` are created (they may reflect zero keys at this point — that is fine).

- [ ] **Step 13: Type-check and lint**

Run: `pnpm type-check && pnpm check`
Expected: no errors.

- [ ] **Step 14: Commit**

```bash
git add package.json pnpm-lock.yaml tsconfig.json lefthook.yml i18next.config.ts src/i18n src/addon.tsx src/types
git commit -m "feat(i18n): add bundled i18next instance and cli tooling"
```

---

### Task 2: Reactive host-language hook

**Files:**
- Modify: `src/lib/addon-sdk-compat.d.ts` (augment `Settings` with `language`)
- Modify: `manifest.json` (add `settings` permission)
- Create: `src/hooks/use-host-language.ts`
- Modify: `src/hooks/index.ts` (re-export)
- Modify: `src/pages/rebalancer.tsx` (call the hook)
- Test: `src/hooks/use-host-language.test.tsx`

**Interfaces:**
- Consumes: `src/i18n/i18n.ts` default instance; `QueryKeys.SETTINGS` and `AddonContext` from `@wealthfolio/addon-sdk`.
- Produces:
  - `normalizeLanguage(language: unknown): string` — returns a supported language code or `'en'`.
  - `useHostLanguage(ctx: AddonContext): string` — reads host settings via React Query, drives `i18n.changeLanguage`, returns the active language.

- [ ] **Step 1: Augment the SDK `Settings` type**

Modify `src/lib/addon-sdk-compat.d.ts`, adding inside the existing `declare module '@wealthfolio/addon-sdk'` block:
```ts
  interface Settings {
    language?: string;
  }
```
(The runtime object from `settings.get()` includes `language`; the v3.6.2 SDK type omits it.)

- [ ] **Step 2: Write the failing unit tests for `normalizeLanguage`**

Create `src/hooks/use-host-language.test.tsx`:
```tsx
// @vitest-environment jsdom
import { describe, expect, it } from 'vitest';
import { normalizeLanguage } from './use-host-language';

describe('normalizeLanguage', () => {
  it('returns a supported language unchanged', () => {
    expect(normalizeLanguage('es')).toBe('es');
  });

  it('collapses regional codes to the base language', () => {
    expect(normalizeLanguage('fr-CA')).toBe('fr');
  });

  it('falls back to en for unsupported languages', () => {
    expect(normalizeLanguage('pt')).toBe('en');
  });

  it('falls back to en for absent/invalid values', () => {
    expect(normalizeLanguage(undefined)).toBe('en');
    expect(normalizeLanguage(null)).toBe('en');
    expect(normalizeLanguage(42)).toBe('en');
  });
});
```

- [ ] **Step 3: Run the test to verify it fails**

Run: `pnpm vitest run src/hooks/use-host-language.test.tsx`
Expected: FAIL — `normalizeLanguage` is not exported / module not found.

- [ ] **Step 4: Implement the hook**

Create `src/hooks/use-host-language.ts`:
```ts
import { useQuery } from '@tanstack/react-query';
import { type AddonContext, QueryKeys } from '@wealthfolio/addon-sdk';
import { useEffect } from 'react';
import i18n, { DEFAULT_LANGUAGE, SUPPORTED_LANGUAGES } from '../i18n/i18n';

export function normalizeLanguage(language: unknown): string {
  if (typeof language !== 'string') {
    return DEFAULT_LANGUAGE;
  }
  const base = language.split('-')[0]?.toLowerCase() ?? DEFAULT_LANGUAGE;
  return (SUPPORTED_LANGUAGES as readonly string[]).includes(base) ? base : DEFAULT_LANGUAGE;
}

export function useHostLanguage(ctx: AddonContext): string {
  const { data } = useQuery({
    queryKey: [QueryKeys.SETTINGS],
    queryFn: () => ctx.api.settings.get(),
    staleTime: 5 * 60 * 1000,
    retry: false,
  });

  const language = normalizeLanguage(data?.language);

  useEffect(() => {
    if (i18n.language !== language) {
      i18n.changeLanguage(language);
    }
  }, [language]);

  return language;
}
```

- [ ] **Step 5: Add the reactive render tests**

Append to `src/hooks/use-host-language.test.tsx`:
```tsx
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import type { AddonContext } from '@wealthfolio/addon-sdk';
import { act } from 'react';
import { createRoot } from 'react-dom/client';
import { vi } from 'vitest';
import i18n from '../i18n/i18n';
import { useHostLanguage } from './use-host-language';

function makeCtx(get: () => Promise<{ language?: string }>): AddonContext {
  return {
    api: { settings: { get } },
  } as unknown as AddonContext;
}

async function mountWithCtx(ctx: AddonContext): Promise<() => void> {
  (globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } });

  function Harness() {
    useHostLanguage(ctx);
    return null;
  }

  const container = document.createElement('div');
  const root = createRoot(container);
  await act(async () => {
    root.render(
      <QueryClientProvider client={client}>
        <Harness />
      </QueryClientProvider>
    );
    await Promise.resolve();
  });
  // Flush the resolved settings query + the changeLanguage effect.
  await act(async () => {
    await Promise.resolve();
    await Promise.resolve();
  });
  return () => act(() => root.unmount());
}

describe('useHostLanguage (reactive)', () => {
  it('switches the instance to the host language', async () => {
    await i18n.changeLanguage('en');
    const ctx = makeCtx(() => Promise.resolve({ language: 'es' }));
    const unmount = await mountWithCtx(ctx);
    expect(i18n.language).toBe('es');
    await unmount();
  });

  it('falls back to en when settings.get rejects', async () => {
    await i18n.changeLanguage('es');
    const ctx = makeCtx(() => Promise.reject(new Error('permission denied')));
    const unmount = await mountWithCtx(ctx);
    expect(i18n.language).toBe('en');
    await unmount();
  });

  it('falls back to en for an unsupported host language', async () => {
    await i18n.changeLanguage('en');
    const ctx = makeCtx(() => Promise.resolve({ language: 'pt' }));
    const unmount = await mountWithCtx(ctx);
    expect(i18n.language).toBe('en');
    await unmount();
  });
});
```

- [ ] **Step 6: Run the tests to verify they pass**

Run: `pnpm vitest run src/hooks/use-host-language.test.tsx`
Expected: all `normalizeLanguage` and `useHostLanguage (reactive)` tests pass.

- [ ] **Step 7: Re-export the hook**

Add to `src/hooks/index.ts`:
```ts
export * from './use-host-language';
```

- [ ] **Step 8: Call the hook in the addon page**

Modify `src/pages/rebalancer.tsx`. Add the import and invoke it at the top of `Rebalancer` so it is active for the whole addon:
```tsx
import { useHostLanguage } from '../hooks';
```
Inside `export function Rebalancer({ ctx }: { ctx: AddonContext })`, add as the first line of the body:
```tsx
  useHostLanguage(ctx);
```

- [ ] **Step 9: Add the `settings` permission to the manifest**

In `manifest.json`, append to the `permissions` array:
```json
    {
      "category": "settings",
      "functions": [
        "get"
      ],
      "purpose": "Read the host UI language to localize the addon"
    }
```

- [ ] **Step 10: Type-check, lint, run the full suite**

Run: `pnpm type-check && pnpm check && pnpm vitest run`
Expected: no errors; all tests pass.

- [ ] **Step 11: Commit**

```bash
git add src/lib/addon-sdk-compat.d.ts src/hooks/use-host-language.ts src/hooks/use-host-language.test.tsx src/hooks/index.ts src/pages/rebalancer.tsx manifest.json
git commit -m "feat(i18n): follow host language reactively via settings"
```

---

### Task 3: Migrate UI strings to `t()` and extract English

**Files:**
- Modify: `src/pages/rebalancer.tsx`
- Modify: `src/components/allocation-overview.tsx`
- Modify: `src/components/transfer-card.tsx`
- Modify: `src/components/holding-planner.tsx`
- Modify: `src/components/account-selector.tsx`
- Generated by extractor: `src/i18n/locales/en/rebalancer.json`, `src/i18n/locales/{fr,de,es,zh}/rebalancer.json` (empty placeholders), `src/types/i18next.d.ts`, `src/types/resources.d.ts`
- Test: `src/components/rebalancer-i18n.test.tsx`

**Interfaces:**
- Consumes: `useTranslation` from `react-i18next`; the `rebalancer` namespace.
- Produces: the English key set below (this is the exact `en/rebalancer.json` after `pnpm i18n:extract`).

**Target English resource (`src/i18n/locales/en/rebalancer.json`):**
```json
{
  "account": {
    "noAccounts": "No accounts found.",
    "searchPlaceholder": "Search accounts...",
    "select": "Select an account",
    "selectPrompt": "Please select an account to identify transfer opportunities."
  },
  "controls": {
    "any": "Any",
    "decreaseThreshold": "Decrease threshold",
    "increaseThreshold": "Increase threshold",
    "pp": "{{value}}pp",
    "threshold": "Threshold"
  },
  "header": {
    "subtitle": "Identify tax-free transfers to keep your portfolio on target"
  },
  "holdings": {
    "addTransactions": "Add Transactions",
    "empty": "No holdings found in this account."
  },
  "overview": {
    "aboveTarget": "Above target",
    "belowTarget": "Below target",
    "biggestGaps": "Biggest gaps",
    "colAllocation": "Allocation",
    "colCurrent": "Current",
    "colDrift": "Drift",
    "colFund": "Fund",
    "colTarget": "Target",
    "gapsEmpty": "Everything is within target. No action required.",
    "gapsSubtitleAll": "Funds off target, largest deviation first.",
    "gapsSubtitleThreshold": "Funds beyond the {{value}}pp threshold.",
    "moreOffTarget": "+{{n}} more off target",
    "subtitleCurrent": "Current allocation of each fund versus its target.",
    "subtitleProjected": "Projected allocation after applying the suggested transfers.",
    "title": "Allocation vs target",
    "viewTransfers": "View transfers to rebalance"
  },
  "plan": {
    "configurePrompt": "Set your target allocations to discover which transfers will bring your portfolio on target.",
    "corrupted": "Your plan needs adjustment. Make sure enabled allocations add up to 100%.",
    "create": "Create Plan",
    "description": "Define your target allocations — the addon will calculate the optimal transfers to reach them.",
    "edit": "Edit Plan"
  },
  "planner": {
    "enableAria": "Enable {{name}} in the plan",
    "save": "Save",
    "saveError": "Failed to save the plan. Please try again.",
    "sumError": "Enabled targets sum to {{total}}%, must equal 100%",
    "targetAria": "Target allocation for {{name}}"
  },
  "transfers": {
    "onTarget": "Your portfolio is on target — no transfers needed.",
    "pctTooltip": "Percentage of the source fund being transferred"
  },
  "view": {
    "current": "Current",
    "projected": "Projected",
    "transfers": "Transfers"
  }
}
```

- [ ] **Step 1: Migrate `src/pages/rebalancer.tsx`**

Add `import { useTranslation } from 'react-i18next';`.

In `EditPlanSheet`, change the default label param and use the hook:
```tsx
export function EditPlanSheet({ ctx, label, compact = false }: EditPlanSheetProps) {
  const { t } = useTranslation();
  const [open, setOpen] = useState(false);
  const resolvedLabel = label ?? t('plan.edit', 'Edit Plan');
```
Replace `{compact ? <span className="hidden sm:inline">{label}</span> : label}` with `resolvedLabel`:
```tsx
          {compact ? <span className="hidden sm:inline">{resolvedLabel}</span> : resolvedLabel}
```
Replace the `SheetTitle`/`SheetDescription`:
```tsx
          <SheetTitle>{t('plan.edit', 'Edit Plan')}</SheetTitle>
          <SheetDescription>
            {t(
              'plan.description',
              'Define your target allocations — the addon will calculate the optimal transfers to reach them.'
            )}
          </SheetDescription>
```

In `RebalancerContent`, add `const { t } = useTranslation();` at the top, then replace:
- `No holdings found in this account.` → `{t('holdings.empty', 'No holdings found in this account.')}`
- `Add Transactions` → `{t('holdings.addTransactions', 'Add Transactions')}`
- `Set your target allocations to discover which transfers will bring your portfolio on target.` → `{t('plan.configurePrompt', 'Set your target allocations to discover which transfers will bring your portfolio on target.')}`
- `<EditPlanSheet ctx={ctx} label="Create Plan" />` → `<EditPlanSheet ctx={ctx} label={t('plan.create', 'Create Plan')} />`
- `Your plan needs adjustment. Make sure enabled allocations add up to 100%.` → `{t('plan.corrupted', 'Your plan needs adjustment. Make sure enabled allocations add up to 100%.')}`
- The second `<EditPlanSheet ctx={ctx} label="Edit Plan" />` → `<EditPlanSheet ctx={ctx} label={t('plan.edit', 'Edit Plan')} />`
- `<span className="text-sm text-muted-foreground">Threshold</span>` → `{t('controls.threshold', 'Threshold')}` inside the span
- `aria-label="Decrease threshold"` → `aria-label={t('controls.decreaseThreshold', 'Decrease threshold')}`
- `aria-label="Increase threshold"` → `aria-label={t('controls.increaseThreshold', 'Increase threshold')}`
- `{tolerancePp === 0 ? 'Any' : \`${tolerancePp.toFixed(1)}pp\`}` →
  ```tsx
  {tolerancePp === 0
    ? t('controls.any', 'Any')
    : t('controls.pp', '{{value}}pp', { value: tolerancePp.toFixed(1) })}
  ```
- Toggle items:
  ```tsx
  items={[
    { value: 'current', label: t('view.current', 'Current') },
    { value: 'transfers', label: t('view.transfers', 'Transfers') },
    { value: 'projected', label: t('view.projected', 'Projected') },
  ]}
  ```
- `Your portfolio is on target — no transfers needed.` → `{t('transfers.onTarget', 'Your portfolio is on target — no transfers needed.')}`

In `Rebalancer`, add `const { t } = useTranslation();` (after `useHostLanguage(ctx);`), and replace the select-account prompt:
```tsx
            {t('account.selectPrompt', 'Please select an account to identify transfer opportunities.')}
```
(Leave `heading={...addonName...}` as-is — the brand name is not translated.) Also wrap the `ApplicationHeader` `text` subtitle:
```tsx
        text={t('header.subtitle', 'Identify tax-free transfers to keep your portfolio on target')}
```

- [ ] **Step 2: Migrate `src/components/allocation-overview.tsx`**

Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation();` inside the component that renders these strings. Replace:
- `Allocation vs target` → `{t('overview.title', 'Allocation vs target')}`
- `Current allocation of each fund versus its target.` → `{t('overview.subtitleCurrent', 'Current allocation of each fund versus its target.')}`
- `Projected allocation after applying the suggested transfers.` → `{t('overview.subtitleProjected', 'Projected allocation after applying the suggested transfers.')}`
- `Fund` → `{t('overview.colFund', 'Fund')}`
- `Allocation` → `{t('overview.colAllocation', 'Allocation')}`
- `Current` (column) → `{t('overview.colCurrent', 'Current')}`
- `Target` (column) → `{t('overview.colTarget', 'Target')}`
- `Drift` → `{t('overview.colDrift', 'Drift')}`
- `Biggest gaps` → `{t('overview.biggestGaps', 'Biggest gaps')}`
- `Funds off target, largest deviation first.` → `{t('overview.gapsSubtitleAll', 'Funds off target, largest deviation first.')}`
- `` `Funds beyond the ${tolerancePp.toFixed(1)}pp threshold.` `` → `{t('overview.gapsSubtitleThreshold', 'Funds beyond the {{value}}pp threshold.', { value: tolerancePp.toFixed(1) })}`
- `{row.status === 'overweight' ? 'Above target' : 'Below target'}` → `{row.status === 'overweight' ? t('overview.aboveTarget', 'Above target') : t('overview.belowTarget', 'Below target')}`
- `Everything is within target. No action required.` → `{t('overview.gapsEmpty', 'Everything is within target. No action required.')}`
- `+{gaps.length - 3} more off target` → `{t('overview.moreOffTarget', '+{{n}} more off target', { n: gaps.length - 3 })}`
- `View transfers to rebalance` → `{t('overview.viewTransfers', 'View transfers to rebalance')}`

- [ ] **Step 3: Migrate `src/components/transfer-card.tsx`**

Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation();` inside `HoldingCard`. Replace the tooltip:
```tsx
            title={t('transfers.pctTooltip', 'Percentage of the source fund being transferred')}
```
(Do not translate `HoldingCardSkeleton` — it has no user-facing text.)

- [ ] **Step 4: Migrate `src/components/holding-planner.tsx`**

Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation();` inside `HoldingPlanner`. Replace:
- `` `Enabled targets sum to ${roundedTotal.toFixed(2)}%, must equal 100%` `` → `t('planner.sumError', 'Enabled targets sum to {{total}}%, must equal 100%', { total: roundedTotal.toFixed(2) })`
- `'Failed to save the plan. Please try again.'` → `t('planner.saveError', 'Failed to save the plan. Please try again.')`
- `` aria-label={`Target allocation for ${holding.instrument?.symbol || holding.holdingType}`} `` → `aria-label={t('planner.targetAria', 'Target allocation for {{name}}', { name: holding.instrument?.symbol || holding.holdingType })}`
- `` aria-label={`Enable ${holding.instrument?.symbol || holding.holdingType} in the plan`} `` → `aria-label={t('planner.enableAria', 'Enable {{name}} in the plan', { name: holding.instrument?.symbol || holding.holdingType })}`
- The `Save` button text → `{t('planner.save', 'Save')}`
- Leave `placeholder="0.00"` and `{roundedTotal.toFixed(2)} / 100%` as literals (numeric, not translatable).

- [ ] **Step 5: Migrate `src/components/account-selector.tsx`**

Add `import { useTranslation } from 'react-i18next';` and `const { t } = useTranslation();` inside the component. Replace:
- `Select an account` → `{t('account.select', 'Select an account')}`
- `placeholder="Search accounts..."` → `placeholder={t('account.searchPlaceholder', 'Search accounts...')}`
- `No accounts found.` → `{t('account.noAccounts', 'No accounts found.')}`

- [ ] **Step 6: Extract keys and generate types**

Run: `pnpm i18n`
Expected: `src/i18n/locales/en/rebalancer.json` now matches the "Target English resource" above; `src/i18n/locales/{fr,de,es,zh}/rebalancer.json` contain the same keys with empty-string values; `src/types/i18next.d.ts` and `src/types/resources.d.ts` are regenerated.

- [ ] **Step 7: Verify the English resource matches**

Run: `cat src/i18n/locales/en/rebalancer.json`
Expected: identical to the "Target English resource" JSON above. If any key is missing, a string in the source was not wrapped — wrap it and re-run `pnpm i18n`.

- [ ] **Step 8: Write the failing English render test**

Create `src/components/rebalancer-i18n.test.tsx`:
```tsx
// @vitest-environment jsdom
import { act, type ReactNode } from 'react';
import { createRoot } from 'react-dom/client';
import { I18nextProvider } from 'react-i18next';
import { describe, expect, it } from 'vitest';
import i18n from '../i18n/i18n';
import { AccountSelector } from './account-selector';

async function renderIn(language: string, node: ReactNode): Promise<string> {
  (globalThis as { IS_REACT_ACT_ENVIRONMENT?: boolean }).IS_REACT_ACT_ENVIRONMENT = true;
  await i18n.changeLanguage(language);
  const container = document.createElement('div');
  const root = createRoot(container);
  await act(async () => {
    root.render(<I18nextProvider i18n={i18n}>{node}</I18nextProvider>);
    await Promise.resolve();
  });
  const text = container.textContent ?? '';
  await act(() => root.unmount());
  return text;
}

describe('AccountSelector i18n (English)', () => {
  it('renders the English empty-selection label', async () => {
    const ctx = {
      api: { accounts: { getAll: () => Promise.resolve([]) } },
    } as unknown as import('@wealthfolio/addon-sdk').AddonContext;
    const text = await renderIn('en', <AccountSelector ctx={ctx} />);
    expect(text).toContain('Select an account');
  });
});
```
> If `AccountSelector` needs additional providers (e.g. `SelectedAccountProvider`, `QueryClientProvider`) to render, wrap `node` with them inside `renderIn` using a real `QueryClient` and the same mocked `ctx`.

- [ ] **Step 9: Run the test to verify it passes**

Run: `pnpm vitest run src/components/rebalancer-i18n.test.tsx`
Expected: PASS.

- [ ] **Step 10: Type-check, lint, full suite**

Run: `pnpm type-check && pnpm check && pnpm vitest run`
Expected: no errors; all tests pass. Typed `t()` keys resolve against the generated `src/types` definitions.

- [ ] **Step 11: Commit**

```bash
git add src/pages/rebalancer.tsx src/components src/i18n/locales src/types
git commit -m "feat(i18n): translate UI strings and extract English catalog"
```

---

### Task 4: Fill fr/de/es/zh translations

**Files:**
- Modify: `src/i18n/locales/fr/rebalancer.json`
- Modify: `src/i18n/locales/de/rebalancer.json`
- Modify: `src/i18n/locales/es/rebalancer.json`
- Modify: `src/i18n/locales/zh/rebalancer.json`
- Test: `src/components/rebalancer-i18n.test.tsx` (add a Spanish case)

**Interfaces:**
- Consumes: the key set produced in Task 3. Every language file MUST contain exactly the same keys as `en/rebalancer.json`.

- [ ] **Step 1: Write `src/i18n/locales/es/rebalancer.json`**

```json
{
  "account": {
    "noAccounts": "No se encontraron cuentas.",
    "searchPlaceholder": "Buscar cuentas...",
    "select": "Selecciona una cuenta",
    "selectPrompt": "Selecciona una cuenta para identificar oportunidades de traspaso."
  },
  "controls": {
    "any": "Cualquiera",
    "decreaseThreshold": "Reducir umbral",
    "increaseThreshold": "Aumentar umbral",
    "pp": "{{value}} pp",
    "threshold": "Umbral"
  },
  "header": {
    "subtitle": "Identifica traspasos libres de impuestos para mantener tu cartera en objetivo"
  },
  "holdings": {
    "addTransactions": "Añadir transacciones",
    "empty": "No se encontraron posiciones en esta cuenta."
  },
  "overview": {
    "aboveTarget": "Por encima del objetivo",
    "belowTarget": "Por debajo del objetivo",
    "biggestGaps": "Mayores diferencias",
    "colAllocation": "Asignación",
    "colCurrent": "Actual",
    "colDrift": "Desviación",
    "colFund": "Fondo",
    "colTarget": "Objetivo",
    "gapsEmpty": "Todo está dentro del objetivo. No se requiere ninguna acción.",
    "gapsSubtitleAll": "Fondos fuera de objetivo, mayor desviación primero.",
    "gapsSubtitleThreshold": "Fondos que superan el umbral de {{value}} pp.",
    "moreOffTarget": "+{{n}} más fuera de objetivo",
    "subtitleCurrent": "Asignación actual de cada fondo frente a su objetivo.",
    "subtitleProjected": "Asignación proyectada tras aplicar los traspasos sugeridos.",
    "title": "Asignación frente a objetivo",
    "viewTransfers": "Ver traspasos para reajustar"
  },
  "plan": {
    "configurePrompt": "Define tus asignaciones objetivo para descubrir qué traspasos ajustarán tu cartera al objetivo.",
    "corrupted": "Tu plan necesita ajustes. Asegúrate de que las asignaciones activas sumen 100 %.",
    "create": "Crear plan",
    "description": "Define tus asignaciones objetivo: el complemento calculará los traspasos óptimos para alcanzarlas.",
    "edit": "Editar plan"
  },
  "planner": {
    "enableAria": "Activar {{name}} en el plan",
    "save": "Guardar",
    "saveError": "No se pudo guardar el plan. Inténtalo de nuevo.",
    "sumError": "Las asignaciones activas suman {{total}} %, deben sumar 100 %",
    "targetAria": "Asignación objetivo para {{name}}"
  },
  "transfers": {
    "onTarget": "Tu cartera está en objetivo: no se necesitan traspasos.",
    "pctTooltip": "Porcentaje del fondo de origen que se traspasa"
  },
  "view": {
    "current": "Actual",
    "projected": "Proyectado",
    "transfers": "Traspasos"
  }
}
```

- [ ] **Step 2: Write `src/i18n/locales/fr/rebalancer.json`**

```json
{
  "account": {
    "noAccounts": "Aucun compte trouvé.",
    "searchPlaceholder": "Rechercher des comptes...",
    "select": "Sélectionner un compte",
    "selectPrompt": "Veuillez sélectionner un compte pour identifier les opportunités de transfert."
  },
  "controls": {
    "any": "Tous",
    "decreaseThreshold": "Diminuer le seuil",
    "increaseThreshold": "Augmenter le seuil",
    "pp": "{{value}} pp",
    "threshold": "Seuil"
  },
  "header": {
    "subtitle": "Identifiez les transferts sans impôt pour garder votre portefeuille sur objectif"
  },
  "holdings": {
    "addTransactions": "Ajouter des transactions",
    "empty": "Aucune position trouvée dans ce compte."
  },
  "overview": {
    "aboveTarget": "Au-dessus de l'objectif",
    "belowTarget": "En dessous de l'objectif",
    "biggestGaps": "Plus grands écarts",
    "colAllocation": "Allocation",
    "colCurrent": "Actuel",
    "colDrift": "Écart",
    "colFund": "Fonds",
    "colTarget": "Objectif",
    "gapsEmpty": "Tout est dans l'objectif. Aucune action requise.",
    "gapsSubtitleAll": "Fonds hors objectif, plus grand écart en premier.",
    "gapsSubtitleThreshold": "Fonds au-delà du seuil de {{value}} pp.",
    "moreOffTarget": "+{{n}} de plus hors objectif",
    "subtitleCurrent": "Allocation actuelle de chaque fonds par rapport à son objectif.",
    "subtitleProjected": "Allocation projetée après application des transferts suggérés.",
    "title": "Allocation vs objectif",
    "viewTransfers": "Voir les transferts pour rééquilibrer"
  },
  "plan": {
    "configurePrompt": "Définissez vos allocations cibles pour découvrir quels transferts ramèneront votre portefeuille sur objectif.",
    "corrupted": "Votre plan nécessite un ajustement. Assurez-vous que les allocations activées totalisent 100 %.",
    "create": "Créer un plan",
    "description": "Définissez vos allocations cibles — l'extension calculera les transferts optimaux pour les atteindre.",
    "edit": "Modifier le plan"
  },
  "planner": {
    "enableAria": "Activer {{name}} dans le plan",
    "save": "Enregistrer",
    "saveError": "Échec de l'enregistrement du plan. Veuillez réessayer.",
    "sumError": "Les allocations activées totalisent {{total}} %, elles doivent égaler 100 %",
    "targetAria": "Allocation cible pour {{name}}"
  },
  "transfers": {
    "onTarget": "Votre portefeuille est sur objectif — aucun transfert nécessaire.",
    "pctTooltip": "Pourcentage du fonds source transféré"
  },
  "view": {
    "current": "Actuel",
    "projected": "Projeté",
    "transfers": "Transferts"
  }
}
```

- [ ] **Step 3: Write `src/i18n/locales/de/rebalancer.json`**

```json
{
  "account": {
    "noAccounts": "Keine Konten gefunden.",
    "searchPlaceholder": "Konten suchen...",
    "select": "Konto auswählen",
    "selectPrompt": "Bitte wählen Sie ein Konto, um Übertragungsmöglichkeiten zu ermitteln."
  },
  "controls": {
    "any": "Beliebig",
    "decreaseThreshold": "Schwellenwert verringern",
    "increaseThreshold": "Schwellenwert erhöhen",
    "pp": "{{value}} pp",
    "threshold": "Schwellenwert"
  },
  "header": {
    "subtitle": "Steuerfreie Übertragungen finden, um Ihr Portfolio im Ziel zu halten"
  },
  "holdings": {
    "addTransactions": "Transaktionen hinzufügen",
    "empty": "Keine Positionen in diesem Konto gefunden."
  },
  "overview": {
    "aboveTarget": "Über dem Ziel",
    "belowTarget": "Unter dem Ziel",
    "biggestGaps": "Größte Abweichungen",
    "colAllocation": "Allokation",
    "colCurrent": "Aktuell",
    "colDrift": "Abweichung",
    "colFund": "Fonds",
    "colTarget": "Ziel",
    "gapsEmpty": "Alles ist im Ziel. Keine Aktion erforderlich.",
    "gapsSubtitleAll": "Fonds außerhalb des Ziels, größte Abweichung zuerst.",
    "gapsSubtitleThreshold": "Fonds über dem Schwellenwert von {{value}} pp.",
    "moreOffTarget": "+{{n}} weitere außerhalb des Ziels",
    "subtitleCurrent": "Aktuelle Allokation jedes Fonds im Vergleich zu seinem Ziel.",
    "subtitleProjected": "Prognostizierte Allokation nach Anwendung der vorgeschlagenen Übertragungen.",
    "title": "Allokation vs. Ziel",
    "viewTransfers": "Übertragungen zum Rebalancing anzeigen"
  },
  "plan": {
    "configurePrompt": "Legen Sie Ihre Zielallokationen fest, um herauszufinden, welche Übertragungen Ihr Portfolio ins Ziel bringen.",
    "corrupted": "Ihr Plan muss angepasst werden. Stellen Sie sicher, dass die aktivierten Allokationen 100 % ergeben.",
    "create": "Plan erstellen",
    "description": "Legen Sie Ihre Zielallokationen fest — das Add-on berechnet die optimalen Übertragungen, um sie zu erreichen.",
    "edit": "Plan bearbeiten"
  },
  "planner": {
    "enableAria": "{{name}} im Plan aktivieren",
    "save": "Speichern",
    "saveError": "Der Plan konnte nicht gespeichert werden. Bitte versuchen Sie es erneut.",
    "sumError": "Aktivierte Zielwerte ergeben {{total}} %, müssen aber 100 % ergeben",
    "targetAria": "Zielallokation für {{name}}"
  },
  "transfers": {
    "onTarget": "Ihr Portfolio ist im Ziel — keine Übertragungen nötig.",
    "pctTooltip": "Prozentsatz des übertragenen Quellfonds"
  },
  "view": {
    "current": "Aktuell",
    "projected": "Prognostiziert",
    "transfers": "Übertragungen"
  }
}
```

- [ ] **Step 4: Write `src/i18n/locales/zh/rebalancer.json`**

```json
{
  "account": {
    "noAccounts": "未找到账户。",
    "searchPlaceholder": "搜索账户...",
    "select": "选择账户",
    "selectPrompt": "请选择一个账户以识别转移机会。"
  },
  "controls": {
    "any": "任意",
    "decreaseThreshold": "降低阈值",
    "increaseThreshold": "提高阈值",
    "pp": "{{value}} 个百分点",
    "threshold": "阈值"
  },
  "header": {
    "subtitle": "找出免税转移，让您的投资组合保持在目标水平"
  },
  "holdings": {
    "addTransactions": "添加交易",
    "empty": "此账户中未找到持仓。"
  },
  "overview": {
    "aboveTarget": "高于目标",
    "belowTarget": "低于目标",
    "biggestGaps": "最大差距",
    "colAllocation": "配置",
    "colCurrent": "当前",
    "colDrift": "偏差",
    "colFund": "基金",
    "colTarget": "目标",
    "gapsEmpty": "一切都在目标范围内。无需操作。",
    "gapsSubtitleAll": "偏离目标的基金，偏差最大者优先。",
    "gapsSubtitleThreshold": "超过 {{value}} 个百分点阈值的基金。",
    "moreOffTarget": "还有 +{{n}} 个偏离目标",
    "subtitleCurrent": "每只基金的当前配置与其目标的对比。",
    "subtitleProjected": "应用建议转移后的预计配置。",
    "title": "配置与目标对比",
    "viewTransfers": "查看转移以重新平衡"
  },
  "plan": {
    "configurePrompt": "设置您的目标配置，以发现哪些转移能让您的投资组合回到目标。",
    "corrupted": "您的计划需要调整。请确保已启用的配置总和为 100%。",
    "create": "创建计划",
    "description": "设置您的目标配置——插件将计算达到目标的最优转移。",
    "edit": "编辑计划"
  },
  "planner": {
    "enableAria": "在计划中启用 {{name}}",
    "save": "保存",
    "saveError": "保存计划失败。请重试。",
    "sumError": "已启用的目标总和为 {{total}}%，必须等于 100%",
    "targetAria": "{{name}} 的目标配置"
  },
  "transfers": {
    "onTarget": "您的投资组合已达目标——无需转移。",
    "pctTooltip": "从源基金转出的百分比"
  },
  "view": {
    "current": "当前",
    "projected": "预计",
    "transfers": "转移"
  }
}
```

- [ ] **Step 5: Verify all languages are complete**

Run: `pnpm exec i18next-cli status`
Expected: 100% for every locale/namespace (no missing keys).

- [ ] **Step 6: Add the Spanish render assertion**

In `src/components/rebalancer-i18n.test.tsx`, add inside the existing `describe`:
```tsx
  it('renders the Spanish empty-selection label', async () => {
    const ctx = {
      api: { accounts: { getAll: () => Promise.resolve([]) } },
    } as unknown as import('@wealthfolio/addon-sdk').AddonContext;
    const text = await renderIn('es', <AccountSelector ctx={ctx} />);
    expect(text).toContain('Selecciona una cuenta');
  });
```

- [ ] **Step 7: Run the test to verify it passes**

Run: `pnpm vitest run src/components/rebalancer-i18n.test.tsx`
Expected: both English and Spanish cases pass.

- [ ] **Step 8: Commit**

```bash
git add src/i18n/locales src/components/rebalancer-i18n.test.tsx
git commit -m "feat(i18n): add fr/de/es/zh translations"
```

---

### Task 5: Final verification and bundle report

**Files:** none (verification only)

- [ ] **Step 1: Run the full test suite**

Run: `pnpm vitest run`
Expected: all tests pass.

- [ ] **Step 2: Type-check and Biome check**

Run: `pnpm type-check && pnpm check`
Expected: no errors.

- [ ] **Step 3: Confirm i18n sync gates are clean**

Run: `pnpm exec i18next-cli extract --sync-primary --ci && pnpm exec i18next-cli types --ci`
Expected: both exit 0 (nothing to update). These are the same gates lefthook runs.

- [ ] **Step 4: Build and record the bundle size**

Run: `pnpm build && ls -lh dist/addon.js && gzip -c dist/addon.js | wc -c`
Expected: build succeeds; note the raw size and gzip byte count. Compare against the pre-i18n baseline (~77 kB raw / ~18 kB gzip) and record the delta in the commit message or PR description.

- [ ] **Step 5: Manual smoke check (optional but recommended)**

Load the addon in the Wealthfolio host, switch the app language in Settings between English and Español, and confirm the Rebalancer UI re-translates live without a reload.

- [ ] **Step 6: Final commit (if any lockfile/build artifacts changed)**

```bash
git add -A
git commit -m "chore(i18n): verify build and record bundle size" || echo "nothing to commit"
```
